[project]
name = "fina-trade"
version = "0.1.0"
description = "FinA trade repository and lifecycle event contract"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
